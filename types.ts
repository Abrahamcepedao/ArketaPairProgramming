export type ClassItem = {
  id: string;
  name: string;
  instructor: string;
  datetime: string;
  capacity: number;
  bookedUsers: number;
};

export type MockUser = {
  id: string;
  name: string;
};
